enum SortingDirection {
  ascending,
  descending,
  none,
}

enum DataType {
  categorical,
  numerical,
  temporal,
  ordinal,
}

enum ChartType {
  bar,
  column,
  line,
  pie,
  scatter,
  pareto,
}